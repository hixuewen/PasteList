<Window x:Class="PasteList.Views.SyncStatusWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="同步状态" Height="450" Width="600"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        ShowInTaskbar="False"
        xmlns:local="clr-namespace:PasteList.Views">
    <!-- 资源 -->
    <Window.Resources>
        <!-- Boolean to Status Converter -->
        <local:BoolToStatusConverter x:Key="BoolToStatusConverter"/>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 状态栏 -->
        <Border Grid.Row="0" Background="#F5F5F5" Padding="15,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock Text="🔄" FontSize="16" Margin="0,0,5,0" VerticalAlignment="Center"/>
                    <TextBlock Text="同步状态:" FontWeight="Bold" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding CurrentStatus}" Margin="10,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <Button Content="立即同步" Width="80" Height="30" Margin="0,0,10,0"
                            Command="{Binding SyncNowCommand}"/>
                    <Button Content="刷新" Width="60" Height="30"
                            Command="{Binding RefreshCommand}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 详细信息区域 -->
        <Border Grid.Row="1" Padding="15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- 统计信息 -->
                <Border Grid.Row="0" Background="#E8F5E9" Padding="10" CornerRadius="4" Margin="0,0,0,15">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Margin="10,0">
                            <TextBlock Text="📊 同步统计" FontWeight="SemiBold" FontSize="12"/>
                            <TextBlock Text="{Binding SyncCount, StringFormat={} 次同步}" FontSize="10" Margin="0,2,0,0"/>
                            <TextBlock Text="{Binding SuccessCount, StringFormat={} 次成功}" FontSize="10" Margin="0,2,0,0" Foreground="Green"/>
                            <TextBlock Text="{Binding ErrorCount, StringFormat={} 次失败}" FontSize="10" Margin="0,2,0,0" Foreground="Red"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1" Margin="10,0">
                            <TextBlock Text="📁 文件信息" FontWeight="SemiBold" FontSize="12"/>
                            <TextBlock Text="{Binding SyncFolder}" FontSize="10" Margin="0,2,0,0"/>
                            <TextBlock Text="{Binding LastSyncTimeText}" FontSize="10" Margin="0,2,0,0"/>
                        </StackPanel>

                        <StackPanel Grid.Column="2" Margin="10,0">
                            <TextBlock Text="⚙️ 配置信息" FontWeight="SemiBold" FontSize="12"/>
                            <TextBlock Text="{Binding SyncTypeText}" FontSize="10" Margin="0,2,0,0"/>
                            <TextBlock Text="{Binding SyncIntervalText}" FontSize="10" Margin="0,2,0,0"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- 历史记录列表 -->
                <TextBlock Grid.Row="1" Text="📝 同步历史记录" FontWeight="SemiBold" FontSize="12" Margin="0,0,0,5"/>
                <Border Grid.Row="1" BorderBrush="#DDDDDD" BorderThickness="1">
                    <DataGrid ItemsSource="{Binding SyncHistory}"
                              AutoGenerateColumns="False"
                              IsReadOnly="True"
                              GridLinesVisibility="Horizontal"
                              HeadersVisibility="Column"
                              SelectionMode="Single"
                              CanUserSortColumns="True"
                              CanUserReorderColumns="False"
                              CanUserResizeColumns="True"
                              FontSize="11">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="时间" Binding="{Binding Timestamp, StringFormat=yyyy-MM-dd HH:mm:ss}"
                                               Width="160" IsReadOnly="True"/>
                            <DataGridTextColumn Header="操作类型" Binding="{Binding OperationType}"
                                               Width="80" IsReadOnly="True"/>
                            <DataGridTextColumn Header="记录数" Binding="{Binding RecordCount}"
                                               Width="60" IsReadOnly="True"/>
                            <DataGridTextColumn Header="状态" Binding="{Binding Success, Converter={StaticResource BoolToStatusConverter}}"
                                               Width="60" IsReadOnly="True"/>
                            <DataGridTextColumn Header="错误信息" Binding="{Binding ErrorMessage}"
                                               Width="*" IsReadOnly="True"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
            </Grid>
        </Border>

        <!-- 按钮区域 -->
        <Border Grid.Row="2" Background="#F5F5F5" Padding="15,10">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="清空历史" Width="80" Height="30" Margin="0,0,10,0"
                        Command="{Binding ClearHistoryCommand}"/>
                <Button Content="关闭" Width="60" Height="30"
                        Click="CloseButton_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>